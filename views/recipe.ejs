<!DOCTYPE html>
<html lang="en-US">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Recipe Information</title>
    <meta name="description" content="Tafadzwa Chingore's Recipe Search Application">
    <meta name="author" content="Tafadzwa Chingore">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?familly=Roboto&display=swap" rel="stylesheet">
    <link href="https://fonts.google.com/specimen/Lobster+Two?query=lobster+two" rel="stylesheet">
    <link rel="stylesheet" href="styles/normalize.css">
    <link rel="stylesheet" href="styles/styles.css">
    <link rel="stylesheet" href="styles/larger.css">
</head>

<body>
    <div class="container">
        <h1>
            <%= recipe.title %>
        </h1>
        <img src="<%= recipe.image %>" alt="<%= recipe.title %>" />
        <h2> Ingredients </h2>
        <ul>
            <% recipe.extendedIngredients.forEach(ingredient=> { %>
                <li>
                    <%= ingredient.original %>
                </li>
                <% }) %>
        </ul>🟰

        <h2>Instructions</h2>
        <ol>
            <% recipe.analyzedInstructions[0].steps.forEach(step=> { %>
                <li>
                    <%= step.step %>
                </li>
                <% }) %>
        </ol>
    </div>
    <footer>
        <p>&copy; Tafadzwa Chingore✨<span id="currentYear"></span></p>
        <p>Last modified: <span id="lastModified"></span></p>
    </footer>
    <script src="scripts/getDates.js"></script>
</body>

</html>